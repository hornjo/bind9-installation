{% for item in bind9_zones_forward %}
zone "{{ item }}" {
    type primary;
    file "/etc/bind/{{ item }}";
};
{% endfor %}

{% for item, value in bind9_zones_reverse %}
zone "{{ item }}" {
    type primary;
    file "/etc/bind/{{ item }}";
};
{% endfor %}

{% for item in bind9_zones_remote %}
zone "{{ domain }}" {
    type forward;
    forwarders {
        {{ remote_dns }};
    };
    forward only;
};
{% endfor %}